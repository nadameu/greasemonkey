{
  "devDependencies": {
    "c8": "^7.12.0",
    "jsdom": "^20.0.1",
    "vitest": "^0.19.1"
  },
  "scripts": {
    "build": "userscript build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest watch"
  }
}
